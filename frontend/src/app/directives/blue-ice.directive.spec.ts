import { BlueIceDirective } from './blue-ice.directive';

describe('BlueIceDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueIceDirective();
    expect(directive).toBeTruthy();
  });
});
